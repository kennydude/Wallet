<resources>

    <color name="HoloOrange">#995100</color>
    <color name="HoloOrangeDarker">#995100</color>
    <color name="HoloOrangeAccent">#ff9f32</color>
    <color name="Grey">#CCC</color>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- colorPrimary is used for the default action bar background -->
        <item name="colorPrimary">@color/HoloOrange</item>

        <!-- colorPrimaryDark is used for the status bar -->
        <item name="colorPrimaryDark">@color/HoloOrangeDarker</item>

        <!-- colorAccent is used as the default value for colorControlActivated
             which is used to tint widgets -->
        <item name="colorAccent">@color/HoloOrangeAccent</item>

        <item name="windowActionBar">false</item>

        <item name="actionBarTabBarStyle">@style/ActionBarStyle</item>
    </style>

    <style name="ActionBarStyle" parent="Widget.AppCompat.ActionBar">
        <item name="android:titleTextStyle">@style/ActionBarTitleText</item>
        <!-- Empty icon -->
        <item name="android:icon">@android:color/transparent</item>\
        <item name="android:textColorPrimary">#FFF</item>
    </style>

    <style name="ActionBarTitleText"
           parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">#FFF</item>
    </style>


</resources>
